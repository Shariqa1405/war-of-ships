export class ships {
  name: string;
  length: number;
  id: number;

  constructor(name: string, length: number, id: number) {
    this.name = name;
    this.length = length;
    this.id = id;
  }
}
